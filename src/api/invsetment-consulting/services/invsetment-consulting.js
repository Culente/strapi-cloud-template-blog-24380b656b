'use strict';

/**
 * invsetment-consulting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::invsetment-consulting.invsetment-consulting');
