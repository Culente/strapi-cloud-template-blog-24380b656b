'use strict';

/**
 * invsetment-consulting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::invsetment-consulting.invsetment-consulting');
